# Shreve II §1.1 精读：Infinite Probability Spaces

用 [math-section-reading-template.md](math-section-reading-template.md) 的方法精读 Shreve《Stochastic Calculus for Finance II》§1.1（书页 1–7）。每个主题：**起止 wording → 原文解读 → 定价框架意义 → 若不成立 → 衍生品变种 → 现实类比**。

对应 [quant-coverage-framework.md](quant-coverage-framework.md) 的 L0→L1 关卡：卷一 §2.1 是 L0 列的公理化（一切有限、可查、可加），本节是 L1 列的入场券（质量进集合，工具只剩积分与极限）。

---

## OCR 符号勘误表（扫描件必读）

| OCR 显示 | 实际符号 |
|---|---|
| `il` / `fl` / `n` | Ω |
| `il00` / `floo` | Ω∞（无穷硬币序列空间） |
| `.r` / `:F` | 𝓕（花体 F，σ-algebra） |
| `JP` / `IP'` | ℙ |
| `u-algebra` | σ-algebra |
| `!`（p = ! 处） | ½ |
| `2(20)…2(23)` | 2^(2⁰)=2, 2^(2¹)=4, 2^(2²)=16, 2^(2³)=256（上标丢失） |
| `Ali` / `AIIH` | Aᶜ / 补集记号丢失 |
| `L::=1` / `U�=1` | Σₙ₌₁^∞ / ∪ₙ₌₁^∞ |

提取工具：`brew install poppler`；`pdftotext -layout -f <首页> -l <末页> file.pdf out.txt`。

---

## 两条贯穿主线（母例）

- **衍生品母例**：股票 S₀ = $100，每期上涨(H)或下跌(T)，无限期细分得到一条价格路径（这正是 Shreve 的抛硬币空间 Ω∞，把 H/T 读成涨/跌）。合约用一张 **digital（数字期权）**：S_T ≥ $110 付 $1，否则 $0。各主题用它的变种。
- **现实母例**：给一个国家**丈量并登记土地**——面积测量、分割地块、逐年加密测绘、两家测绘局争地。各主题用它的变种。

---

## 不可数：定义与两条触发路线（§1.1 选例的底层逻辑）

### 什么叫"不可数"

一个集合**可数** = 元素能与正整数一一对应，即**能排成序列 ω₁, ω₂, ω₃, … 逐个点名不漏项**（Shreve 原文的操作性说法："not possible to list their elements in a sequence" 的反面）。**不可数 = 任何点名方案都必然漏项**——不是"没找到好方案"，是"方案不存在"本身可以被证明。

证明武器是 **Cantor 对角线**，一句话版：假设有人把 [0,1] 列成了表，构造一个数——第 n 位故意与表上第 n 个数的第 n 位不同——它不在表上任何位置，点名表作废。同一招原封不动用于硬币序列：把"第 n 条序列的第 n 次抛掷"翻面，得到一条不在任何列表里的序列。§1.1 的两个例子被**同一把刀**证明不可数。

两个易踩的坑：

1. **可数 ≠ 有限**。可数包括有限与可数无穷（ℕ、几何分布的 Ω）。分水岭是"能不能列成表"，不是"多不多"。
2. **稠密 ≠ 不可数**。有理数 ℚ 在数轴上密密麻麻，却可数（按分母分子之和排队点名）。[0,1] 不可数不是因为"挤"，是对角线堵死了一切点名方案。

这个定义是全节的根：**可数 = "逐点遍历"动作存在 → Σ 求和有意义；不可数 = 遍历动作不存在 → 求和无从下笔** → 只能对集合直接赋值 → Definition 1.1.2 为什么长那个样子。

### 不可数的两条触发路线

一个公式统一 §1.1 的两个例子：**|Ω| = (每步选择数)^(步数)**。

- **路线 1**（Example 1.1.3，[0,1] 取数）：**底数大**——不可数^1。单个动作的选择集本身就是 continuum
- **路线 2**（Example 1.1.4，Ω∞）：**指数大**——2^∞。单步只有 2 种选择，无穷次方把它抬成不可数

| 底数 \ 指数 | 有限步 | 无穷步 |
|---|---|---|
| **2**（硬币） | 2^N 有限 ✅ 安全（卷一二叉树） | 2^∞ **不可数** ⚠️ 路线 2 |
| **可数**（选正整数） | 可数 ✅ 安全 | 不可数 ⚠️ |
| **不可数**（选实数） | **不可数** ⚠️ 路线 1（一步就中招） | 不可数 ⚠️ |

危险触发条件：**底数不可数，或（底数 ≥ 2 且指数无穷）**——两个例子各踩中一个。注意措辞：两个例子的共同点严格说是**不可数**无限，不是"无限"——可数无穷（选正整数、几何分布）也是无限，却点上有正质量、求和照常工作，不需要本节的新机器。

Shreve 的选例意图：让两种触发方式各表演一次，然后（经二进制展开 H=1, T=0）两条路线落进**同一个 continuum**——一条无穷涨跌记录 ↔ [0,1] 上一个点的坐标。这个同构也是本文两条母例（衍生品住路线 2、国土丈量住路线 1）能逐主题平行推进的原因。

---

## 主题 1：动机——质量离开点，测量必须重建

**起**「An infinite probability space is used to model…」 ｜ **迄**「…the uniform probability measure on [0,1].」（书页 1–2）

**原文解读**：两个实验贯穿全节：(i) 从 [0,1] 随机取数；(ii) 无穷抛硬币 Ω∞ (1.1.1)。论证链：两空间**不可数** → 单个 outcome 概率为零 → 卷一 (2.1.5) 的"对点求和得事件概率"失效 → 必须**直接给事件赋概率**；但事件无穷多、有些描述极复杂 → 策略改为"只对简单事件给公式，其余靠概率测度的性质延展"。最后这句是整节的施工图。

**定价框架意义**：连续时间金融的样本空间就是路径空间 C[0,T]。任一条具体轨迹概率为零，可定价的只有路径**集合**（S_T > K、max S > B）。定价 = 对路径集合上的 payoff 积分，绝非逐条求和。

**若不成立**：若坚持点上有质量，模型只能是格点/树（卷一的世界）；树本身没错，但它到连续极限的通道正是本节，没有这一节，二叉树价格收敛到 BS 无处落脚。

**衍生品变种**：连续时间里 P(S_T 恰好 = $110.00) = 0。"只在 S_T 精确等于 $110 时付 $1"的点式合约价值为 **$0**；能交易的 digital 定价的是**事件** {S_T ≥ 110}（一段区间），不是那个点。

**现实类比**：一个 GPS 坐标点圈出的面积为零——你无法拥有或估价"一个点的土地"，只能登记**地块**（区域）。地价住在地块上，不住在点上。

---

## 主题 2：Definition 1.1.1 — σ-algebra 三公理

**起**「Definition 1.1.1. Let Ω be a nonempty set, and let 𝓕 be a collection of subsets of Ω.」 ｜ **迄**「…Ω must be one of the sets in 𝓕 because Ω = ∅ᶜ.」（书页 2）

**原文解读**：三条公理：∅ ∈ 𝓕、补集封闭、**可数**并封闭。Shreve 随后推三个衍生性质：有限并封闭（往序列塞 ∅）、可数/有限交封闭（De Morgan）、Ω ∈ 𝓕。刻意的"公理最小化"美学：交集封闭不是公理，是赠品。

**定价框架意义**：𝓕 是**可以写进 term sheet 的事件全集**。补集封闭 = 任何可保险事件其反面也可保险（否则"违约"可定价而"存活"不可定价，CDS 与 risky bond 平价写不出）；可数并封闭 = 复杂条款可由简单条款拼装（barrier = 可数个"某时刻前触碰"之并）。Arrow-Debreu 证券价 e^(−rT)Q(A) 要求 A ∈ 𝓕——**𝓕 就是可定价 claim 的目录**。

**若不成立**：补集不封闭 → price(A) + price(Aᶜ) = 贴现债券的恒等式单边缺失，静态套利检查失去对象；可数并不封闭 → 逐月观察逼近连续观察的极限产品不在目录里（能定价每个近似，却不能定价极限）。

**衍生品变种**：term sheet 写的 A = {S_T ≥ 110} 必须 ∈ 𝓕；由补集封闭，Aᶜ = {S_T < 110} 也在。于是 **digital-up + digital-down = $1 = 一张零息债券**——补集封闭正是两半 digital 能拼回债券的原因。

**现实类比**：登记处只承认某些形状为合法**地块**（𝓕）。规矩：一块地能登记，它**外面**的地也能登记；一块地和它的补territory 拼起来 = 全国面积（那张"债券"）。

---

## 主题 3：Definition 1.1.2 — 测度与可数可加

**起**「Definition 1.1.2. Let Ω be a nonempty set, and let 𝓕 be a σ-algebra of subsets of Ω.」 ｜ **迄**「…a probability measure must satisfy (1.1.3)–(1.1.6).」（书页 2–3）

**原文解读**：ℙ: 𝓕 → [0,1]，两条公理：ℙ(Ω)=1、可数可加 (1.1.2)；三元组 (Ω, 𝓕, ℙ) 命名 probability space。先向下兼容（有限 Ω + 全子集退化为卷一 Def 2.1.1），再推四条会计恒等式：ℙ(∅)=0 (1.1.3)、两两可加 (1.1.4)、有限可加 (1.1.5)、补集规则 (1.1.6)。**背景**：不要"任意可加"因它强迫全空间测度为 0（与主题 1 矛盾）；有限可加不够因极限定理（MCT/DCT）需要可数可加——本章后面才兑现。

**定价框架意义**：风险中性测度 Q 就是这样一个 ℙ，"概率"读作 Arrow-Debreu 价格。可数可加 = **价格在可数分解下守恒** = 无穷精细化组合不产生套利，也是数值方法收敛（二叉树→BS、离散→连续 barrier）的合法性来源。

**若不成立**：只有有限可加时存在"质量漏到无穷远"的测度——一列产品价格的极限 ≠ 极限产品的价格，doubling-strategy 悖论进场；期望论（从而一切 E^Q[·] 定价）随 MCT/DCT 一起塌。

**衍生品变种**：把 110 的 call 拆成 digital 阶梯 C ≈ Σₖ [S_T ≥ 110+kΔ 付 $1]·Δ。**可数可加 = 阶梯总价 = 各 digital 价格之和**；Δ→0 时近似价收敛到 call 真价——"细分再加总不产生套利"的收敛牌照就是可数可加。

**现实类比**：把一块地分成（可数）多个小地块——各小块面积**精确加总**等于母地块面积。无论切多细（可数刀），面积不凭空产生也不丢失。

---

## 主题 4：Example 1.1.3 — [0,1] 上的 Lebesgue 测度与 Borel σ-algebra

**起**「Example 1.1.3 (Uniform (Lebesgue) measure on [0,1]).」 ｜ **迄**「…in terms of unions, intersections, and complements of sequences of closed intervals.」（书页 3–4，含 Cantor set 脚注）

**原文解读**：施工图第一次执行：简单事件 = 闭区间，ℙ[a,b] = b−a (1.1.7)；单点概率零 → 开闭区间同测度 (1.1.8)；由闭区间按 σ-algebra 公理补全，得 **Borel σ-algebra 𝓑[0,1]**。**背景定理（Shreve 隐去）**：从区间长度延展到全体 Borel 集且唯一，靠 **Carathéodory 延拓定理**——"it turns out" 背后是它。脚注引 Cantor set 提示 Borel 集可以很病态。

**定价框架意义**：两个身份。其一，([0,1], 𝓑, Lebesgue) 是**所有 Monte Carlo 的原料仓**——任何分布 = uniform 的逆 CDF 推前。其二，ℝ 上的 Borel σ-algebra 是 **payoff 函数的标准登记册**：E^Q[f(S_T)] 要求 f 是 Borel 可测（call/put/digital 全是），所以实务从不构成约束，但它是期望有定义的前提。

**若不成立**：payoff 非 Borel 可测 → E^Q[f(S_T)] 字面上无定义——不是"算不出"，是"价格"这个词失去指称。

**衍生品变种**：用模拟给 digital 定价——抽 U ~ Uniform[0,1]，令 S_T = F⁻¹(U)（对数正态逆 CDF），对 1{S_T≥110} 取平均。**那个 uniform 抽样就是 [0,1] 上的 Lebesgue 测度**；payoff 是区间示性函数（Borel 可测），期望有定义。

**现实类比**：基准尺子是**面积/长度**；任何正常地块由矩形经并/交搭成。登记处规则书（Borel）说：钉死矩形面积，每块可描述地块的面积随之唯一确定。

### 补充：什么是 Measure——本质与三层分类

**Measure 的本质**：一个**吃集合、吐数字**的函数 μ: 𝓕 → [0,∞]。按"函数三件套"拆开 Def 1.1.2，每个条款管一个部件：

| 函数部件 | 条款 | 条款性质 |
|---|---|---|
| 定义域 | 必须是 σ-algebra | 结构性**前提**——保证行为条款里 μ(∪ₙAₙ)、μ(Aᶜ) 每一项都有定义（𝓕 的封闭公理为此存在） |
| 值域 | [0, ∞]（概率测度经 μ(Ω)=1 缩到 [0,1]） | 输出范围 |
| **赋值行为** | **μ(∅)=0 + 可数可加** | **函数方程**——不规定任何单个取值，只规定不同输入的输出之间的一致性 |

两个高频误区：

- **Measure ≠ metric**（测度 ≠ 度量/距离）：测度吃**一个集合**，metric（如 L² 距离）吃**两个点**——不同物种、不同公理（可数可加 vs 三角不等式）。可加性强迫测度是"内含量"（各部分放在哪不影响读数：Leb([0,1]∪[2,3]) = Leb([0,1]∪[100,101]) = 2），而直径/距离是"构型量"（同一场搬家让 diam 从 3 跳到 101）——**可加性 = 禁止函数记住"关系"**，所以任何想存距离的集合函数永远过不了测度资格审查。
- **公理只管一致性、不管取值**：满足公理只拿到"头衔"；要钉死一个具体测度还需"身份证"——**测度从不被逐值定义，永远被性质钉死**（性质组 + 唯一性定理）。

**三层分类**（同一个词可能住不同层，不可混排）：

**第一层 · 头衔**（内在条款，看自己就能验证）：测度（两条公理）⊃ 有限测度（μ(Ω)<∞）⊃ 概率测度（μ(Ω)=1）。

**第二层 · 个体**（专有名词，被身份证唯一钉死的具体函数）：

| 个体 | 定义域 | 身份证（钉死它的性质） | 持有的头衔 |
|---|---|---|---|
| 计数测度 | 2^Ω | μ(A) = #A | 测度（Ω 有限时兼有限测度） |
| Dirac δ_x | 任意 𝓕 | μ(A) = 1{x ∈ A} | 概率测度 ✓ |
| **Lebesgue on ℝ** | 𝓑(ℝ) | 平移不变 + [0,1]↦1 | 只有"测度"（总质量 ∞，进不了内圈） |
| Lebesgue 裁剪到 [0,1]（= uniform） | 𝓑[0,1] | 同上，裁剪 | 概率测度 ✓ |
| 高斯测度 N(0,1) | 𝓑(ℝ) | 密度 φ 相对 Lebesgue | 概率测度 ✓ |
| 卷一 N 次硬币的 ℙ | 2^Ω | 每点 ↦ p^#H q^#T | 概率测度 ✓ |
| Ω∞ 上的 ℙ_p | 𝓕∞ | cylinder ↦ p^#H q^#T | 概率测度 ✓ |

**第三层 · 岗位**（关系条款，离开模型语境无从谈起；同一岗位在不同模型里由不同个体出任）：

| 岗位 | 聘书（关系条款） | 谁来出任（举例） |
|---|---|---|
| 真实世界测度 ℙ | 与物理频率/历史数据一致 | 模型选定的某个概率测度个体 |
| 分布 μ_X | = ℙ ∘ X⁻¹（相对某个 X 和 ℙ） | X ~ 标准正态时由个体 N(0,1) 出任 |
| 风险中性 Q | Q ~ ℙ 且贴现价格在 Q 下是鞅（相对市场） | BS 模型里由"使贴现 GBM 成鞅的对数正态路径测度"出任 |

三层验证方式各不同：**头衔看条款**（拿 μ 算一遍）、**个体看身份证**（性质 + 唯一性定理，世上只此一人）、**岗位看聘书**（"风险中性测度"单独拎出来不指任何具体函数，如同"值班经理"不指任何具体的人）。Lebesgue 与"概率测度"不是平行关系：一个是第二层个体，一个是第一层头衔——而 Lebesgue 的 [0,1] 裁剪版恰好持有这个头衔。

---

## 主题 5：Example 1.1.4 前半 — 𝓕ₙ 阶梯：filtration 的实物

**起**「Example 1.1.4 (Infinite, independent coin-toss space).」 ｜ **迄**「…every set that can be described in terms of finitely many tosses.」（书页 4–5）

**原文解读**：施工图第二次执行，简单事件换成 **cylinder sets**（"前 n 次抛掷指定"的集合）。𝓕₀={∅,Ω} (1.1.9) → 𝓕₁ (1.1.10) → 𝓕₂ 共 2^(2²)=16 个 (1.1.12) → 𝓕₃ 共 256 个。赋值 (1.1.11)：ℙ(A_HH)=p² 等——**正是卷一的 p^#H q^#T，但承载者从"点的质量"换成"cylinder set 的面积"**。**背景定理（"it turns out" 藏）**：有限维赋值唯一延拓到 𝓕∞ 是 **Kolmogorov/Carathéodory 延拓**，前提是各层赋值**自洽**。

**定价框架意义**：嵌套阶梯 𝓕₀⊂𝓕₁⊂𝓕₂… 是 **filtration 的第一次现身**——信息随时间变细，𝓕ₙ = 前 n 期市场历史可分辨的事件；卷二 §2.1 正式定义前这里已让你亲手搭了一座。它也是"卷一二叉树嵌入连续大厦"的接口：树上每节点 = 一个 cylinder set。

**若不成立**：各层赋值不自洽 → 不存在任何延拓——金融同构物是 **calendar arbitrage**：各到期日 vanilla 报价互相矛盾时，不存在任何 martingale 模型能同时拟合。校准失败的最深层版本在这一页。

**衍生品变种**：改成 knock-out digital——S_T ≥ 110 **且**每月观察日股价从未跌破 $90 才付 $1。𝓕ₙ = n 次月度观察后已知信息；"期权此刻是否还活着"在每个观察日 **𝓕ₙ-可测**。嵌套 𝓕ₙ = 信息逐月累积，二叉树每节点 = 一个 cylinder set。

**现实类比**：测绘局每年把分辨率**翻倍**。第 n 年地图（𝓕ₙ）只能分辨到某网格，次年把每格再细分。这串越来越细的嵌套地图 = 地形信息逐年增加——每年能回答更多问题，绝不更少。

---

## 主题 6：Example 1.1.4 后半 — 零概率路径与 SLLN 集（互奇异测度）

**起**「Once the probabilities of all these sets are specified…」 ｜ **迄**「…it is extremely difficult to produce a set not in 𝓕∞, although such sets exist.」（书页 5–7）

**原文解读**：三步。(a) 单序列 HHHH… 不可由有限次抛掷描述，但被 A_H ⊃ A_HH ⊃… 夹住，ℙ ≤ pⁿ → 0，故**每条单一序列概率为零**（主题 1 的断言在此获证）。(b) 定义 𝓕∞ = 含全部有限描述集的最小 σ-algebra。(c) 展示"描述涉及无穷次抛掷"的事件 A = {H 频率 → ½} (1.1.13)，显式写成 **A = ∩ₘ ∪_N ∩_{n≥N} A_{n,m}**——三层可数运算翻译 ∀m∃N∀n≥N。SLLN 断言 ℙ(A)=1 当 p=½、=0 当 p≠½。末句：𝓕∞ 之外的集合存在但"极难构造"（背景：Vitali 型不可测集，需选择公理）。

**定价框架意义**：全节对定价**最锋利**的一段。p=½ 与 p=⅓ 两测度定义在同一 Ω∞ 上，却各把全部质量放在互不相交的频率集上——**mutually singular**。连续时间同构命题：**vol 不同的两个测度在路径空间上互奇异**——quadratic variation/t 沿单条路径 a.s. 识别 σ²。于是那条 desk 金句的出处：**drift 可谈判（Girsanov 换测度，等价性保持），vol 不可谈判（换 vol = 换到奇异测度）**。FTAP 要求 Q ~ P（同 null 集），根就在这里。

**若不成立**：若允许与真实测度奇异的 Q 定价，就给 P-不可能事件赋正价值——直接套利。风险中性定价"只能改 drift、不能改 vol"的边界是本段推论。

**衍生品变种**：同一 digital 在 σ=20% 与 σ=30% 下定价。两路径测度**互奇异**——从**一条**观察到的路径算 realized quadratic variation 就能 a.s. 读出真 σ。换测度换不动 vol（Girsanov 只挪 drift）：**drift 可谈判，vol 不可以**。

**现实类比**：两家测绘局用不兼容的基准尺——一家只登记**旱地**，一家只登记**潮汐水域**，在一片两栖三角洲各说各话。每家判定对方的"整个辖区"是零测怪胎：两者满测度领土互不相交。拿到真地形你能确定手上是哪家的图，却无法把一张图**等价缩放**成另一张。

---

## 主题 7：悖论与 almost surely

**起**「The observation in Example 1.1.4 that every individual sequence has probability zero highlights a paradox…」 ｜ **迄**「Definition 1.1.5. … we say that the event A occurs almost surely.」（书页 7）

**原文解读**：悖论——全 H 序列概率为零，但它在样本空间里，且不比任何其他单序列"更不可能"（大家都是零）。数学界的解决是造一个"含糊其辞"的术语（Shreve 原话自嘲）：概率为 1 的事件 **almost surely** 发生（Definition 1.1.5）。

**定价框架意义**：连续时间金融的核心命题都只在 a.s. 意义下成立：**复制对冲的 P&L = 0 是 a.s. 的，不是逐路径的**；两个 a.s. 相等的 payoff 同价（payoff 是 a.s. 等价类）；连"无套利"定义本身都用 a.s. 语言写（V₀=0, ℙ(V_T≥0)=1, ℙ(V_T>0)>0）。没有 Def 1.1.5，"复制"与"套利"两词都无法陈述。

**若不成立**：要求逐路径（surely）成立的对冲理论是空集——delta 对冲在零测路径集上本就可失败，坚持 surely 意味着 BS 复制论证不成立，整卷失去主定理。

**衍生品变种**：delta 对冲这张 digital，对冲 P&L → 0 **几乎必然**——唯独在 S_T 精确落在 $110 的刀刃路径上失效（digital 不连续处的 **pin risk**，一个零测路径集）。"复制 a.s. 成立" = 除概率零的路径集外都成立，与 Shreve"至少一个 T，almost surely"完全同构。

**现实类比**："全国已测绘完毕"指面积为 1 的部分**几乎处处**覆盖——除一条零面积的分形**海岸线**，任何有限次测绘都钉不死它。国家 almost surely 被测完，尽管并非字面上每个点都落在某地块里。

---

## 一句话放回框架

卷一 §2.1 = L0 列的公理化（有限、可查、可加）；本节 §1.1 = L1 列的入场券（质量进集合，工具只剩积分与极限）。给大二学生的 Phase 2 桥梁，第一站就是这两节对照读。
